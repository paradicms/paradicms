query InstitutionOverviewQuery($institutionUri: String!) {
    institutionByUri(uri: $institutionUri) {
        collections {
            description
            name
            uri
        }
        name
    }
}
