#import "./objectSummaryFragment.graphql"

query CollectionOverviewObjectsPaginationQuery($collectionUri: String!, $limit: Int!, $offset: Int!) {
    collectionByUri(uri: $collectionUri) {
        objects(limit: $limit, offset: $offset) {
            ...ObjectSummaryFragment
        }
    }
}
