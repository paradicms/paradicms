{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "scripts": {
    "lerna:bootstrap": "set EDITOR=\"\" && lerna bootstrap --hoist",
    "lerna:publish": "lerna publish"
  }
}
