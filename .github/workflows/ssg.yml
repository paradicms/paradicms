name: Build and test GUI Python
on:
  push:
    paths:
      - ".github/workflows/ssg.yml"
      - "ssg/**"
  workflow_dispatch:
jobs:
  build:
    name: Build and test Python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: "3.9.x"
      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-in-project: true
      - name: Load cached venv
        id: cached-poetry-dependencies
        uses: actions/cache@v2
        with:
          path: ssg/.venv
          key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('ssg/poetry.lock') }}
      - name: Install dependencies
        if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
        run: cd ssg && poetry install --no-interaction --no-root
      - run: cd ssg && poetry run black --check paradicms_ssg
      - run: cd ssg && poetry run flake8 paradicms_ssg
      - run: cd ssg && poetry run mypy paradicms_ssg
